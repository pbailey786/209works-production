'use client ' import { useParams } from 'next/navigation'; export default function UpgradeJobPage() { { const params = useParams(); return ( // Note: Multiple root elements may need React.Fragment wrapping; <div className="mx-auto max-w-2 xl px-4 py-1 6 text-center">" " <h1 className="mb-4 text-3 xl font-bold">Upgrade Job Post</h1>" " <p className="text-gray-7 00"> } " " Upgrade job post ID: <span className="font-mono"> { params.id } </span> to; "" featured or top of list. </p> </div> )}